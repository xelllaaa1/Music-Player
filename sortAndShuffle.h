// A function prototype of a tiny helper which converts uppercase chars to lowercase chars using
// ASCII arithmetic.
char tolowerchar(char ch);

// A function prototype to compare 2 strings ignoring case.
int compareStrings(char string1[], char string2[]);

// This method sorts the artists alphabetically.
void selectionSort(char targetArray[][80] , int arraySize);

// A function prototype of a tiny helper function to swap 2 elements of a 2D character array.
void swap(char a[], char b[]);

// This method shuffles a set of songs.
void shuffleSongs(char songsToBeShuffled[24][160], int numOfSongs);